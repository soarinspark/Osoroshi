# This script file is for defining characters.
# The character variable names are 'c_', followed by the character's initials.

init:

   define c_mc = Character("Meiko Chiba")
   define c_fn = Character("Frickin' Nerd")